package com.ldh.portfolio.domain.project;

public enum DisplayMode {
    INTERNAL,  // open header detail (list its ProjectItems)
    LIVE,      // open liveUrl
    GITHUB,    // open githubUrl
    DOCS,      // open docsUrl
    VIDEO      // open videoUrl
}
